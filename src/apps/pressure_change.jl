"""

"""
function app_pressure_change(ms::MaterialStream, Δp::Float64, η::Float64)
    Uᵢₙ = mstream_U(ms)
    Dᵢₙ = mstream_D_mass(ms)
    Hᵢₙ = mstream_H(ms)
    println("U ", Uᵢₙ)
    println("D ", Dᵢₙ)
    println("H ", Hᵢₙ)
    N = 100
    for i = 1:N
        
    end

end
